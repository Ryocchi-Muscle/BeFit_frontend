// import { getSession } from "next-auth/react";
// interface Session{
//   accessToken: string;
// }


// export async function secureApiCall(endpoint, method = "GET", body = null) {
//   const session = await getSession() as Session;
//   const token = session?.accessToken;

//   return ;
// }
