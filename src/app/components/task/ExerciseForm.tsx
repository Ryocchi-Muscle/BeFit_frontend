// components/todo/ExerciseForm.tsx
import React from "react";

const ExerciseForm = () => {
  // ここでフォームのロジックを追加
  return (
    <form>
      {/* 種目No., 部位, セット数, 重量, 回数, 完了マークを含むフォーム要素をここに追加 */}
      <p>Exercise Form Placeholder</p>
    </form>
  );
};

export default ExerciseForm;
